"use client";

/**
 * SelectionOverlay provides visual feedback for the selected cell/range.
 * The active cell and selection range styling are handled inline in Cell
 * and the grid structure. This component can be extended later for
 * drag-handles or more complex overlays. For Phase 4, the selection
 * styling is in Cell and ColumnHeader/RowHeader.
 */
export function SelectionOverlay() {
  return null;
}
